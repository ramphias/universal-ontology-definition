{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/ramphias/universal-ontology-definition/schema/core_schema.json",
  "title": "Universal Ontology Core Schema",
  "description": "JSON Schema for validating L1 Universal Enterprise Ontology Core definitions. v2.0 adds abstract classes, lifecycle status, cardinality, and deprecation tracking.",
  "type": "object",
  "required": ["metadata", "classes", "relations", "attributes"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Reference to this schema file"
    },
    "metadata": {
      "type": "object",
      "required": ["name", "layer", "version"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable name of the ontology"
        },
        "layer": {
          "type": "string",
          "pattern": "^L1_",
          "description": "Layer identifier, must start with L1_"
        },
        "version": {
          "type": "string",
          "pattern": "^\\d+\\.\\d+\\.\\d+$",
          "description": "Semantic version string"
        },
        "license": {
          "type": "string"
        },
        "namespace": {
          "type": "string",
          "format": "uri"
        },
        "description": {
          "type": "string"
        },
        "description_en": {
          "type": "string"
        },
        "created": {
          "type": "string",
          "format": "date"
        },
        "updated": {
          "type": "string",
          "format": "date",
          "description": "Last modification date"
        },
        "contributors": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "classes": {
      "type": "array",
      "minItems": 1,
      "maxItems": 25,
      "description": "L1 Core classes, hard capped at 25 to prevent entropy growth (Governance Rule G-01)",
      "items": {
        "type": "object",
        "required": ["id", "label_zh", "definition"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[A-Z][a-zA-Z0-9]*$",
            "description": "PascalCase class identifier"
          },
          "label_zh": {
            "type": "string",
            "minLength": 1,
            "description": "Chinese label"
          },
          "label_en": {
            "type": "string",
            "description": "English label"
          },
          "parent": {
            "type": ["string", "null"],
            "description": "Parent class ID or null for abstract root domains only"
          },
          "abstract": {
            "type": "boolean",
            "default": false,
            "description": "Abstract classes cannot be directly instantiated; they serve as structural groupings for inheritance"
          },
          "status": {
            "type": "string",
            "enum": ["stable", "experimental", "deprecated"],
            "default": "stable",
            "description": "Lifecycle status of this class"
          },
          "since": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$",
            "description": "Version when this class was first introduced"
          },
          "deprecated_since": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$",
            "description": "Version when this class was deprecated"
          },
          "replaced_by": {
            "type": ["string", "null"],
            "description": "ID of the class that replaces this one, if deprecated"
          },
          "replaced_from": {
            "type": ["string", "null"],
            "description": "ID of the class that this one replaces"
          },
          "definition": {
            "type": "string",
            "minLength": 1,
            "description": "Chinese definition"
          },
          "definition_en": {
            "type": "string",
            "description": "English definition"
          }
        }
      }
    },
    "attributes": {
      "type": "array",
      "description": "Data properties (attributes) that describe intrinsic characteristics of classes. Unlike relations (which link class to class), attributes assign primitive-typed values to instances.",
      "items": {
        "type": "object",
        "required": ["id", "owner_class", "datatype", "definition"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "snake_case attribute identifier"
          },
          "label_zh": {
            "type": "string",
            "description": "Chinese label"
          },
          "label_en": {
            "type": "string",
            "description": "English label"
          },
          "owner_class": {
            "type": "string",
            "pattern": "^[A-Z][a-zA-Z0-9]*$",
            "description": "The class this attribute belongs to. Subclasses inherit it."
          },
          "datatype": {
            "type": "string",
            "enum": ["string", "integer", "decimal", "boolean", "date", "datetime", "uri", "enum"],
            "description": "Primitive data type of the attribute value"
          },
          "required": {
            "type": "boolean",
            "default": false,
            "description": "Whether this attribute is mandatory for instances of the owner class"
          },
          "enum_values": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Allowed values when datatype is 'enum'"
          },
          "status": {
            "type": "string",
            "enum": ["stable", "experimental", "deprecated"],
            "default": "stable",
            "description": "Lifecycle status"
          },
          "since": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$",
            "description": "Version when this attribute was first introduced"
          },
          "deprecated_since": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$",
            "description": "Version when this attribute was deprecated"
          },
          "replaced_by": {
            "type": ["string", "null"],
            "description": "ID of the attribute that replaces this one, if deprecated"
          },
          "definition": {
            "type": "string",
            "minLength": 1,
            "description": "Chinese definition"
          },
          "definition_en": {
            "type": "string",
            "description": "English definition"
          }
        }
      }
    },
    "relations": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "domain", "range", "definition"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "snake_case relation identifier"
          },
          "label_zh": {
            "type": "string"
          },
          "label_en": {
            "type": "string"
          },
          "domain": {
            "type": ["string", "null"],
            "description": "Source class ID — prefer abstract parent classes to enable L2 reuse. Null implies universal applicability."
          },
          "range": {
            "type": ["string", "null"],
            "description": "Target class ID — prefer abstract parent classes to enable L2 reuse. Null implies universal applicability."
          },
          "cardinality": {
            "type": "string",
            "enum": ["1:1", "1:N", "N:1", "N:M"],
            "default": "N:M",
            "description": "Relationship cardinality"
          },
          "inverse_of": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "ID of the inverse relation, if defined"
          },
          "since": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$",
            "description": "Version when this relation was first introduced"
          },
          "deprecated_since": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$"
          },
          "replaced_by": {
            "type": ["string", "null"]
          },
          "definition": {
            "type": "string",
            "minLength": 1
          },
          "definition_en": {
            "type": "string"
          }
        }
      }
    },
    "migration_registry": {
      "type": "array",
      "description": "Historical registry of classes, relations, or attributes that have been removed from their arrays. Provides migration guidance for downstream consumers. Items still present in their arrays with status='deprecated' do NOT need an entry here — this is only for fully removed items.",
      "items": {
        "type": "object",
        "required": ["id", "kind", "deprecated_since"],
        "properties": {
          "id": {
            "type": "string",
            "description": "Original ID of the removed item"
          },
          "kind": {
            "type": "string",
            "enum": ["class", "relation", "attribute"],
            "description": "What type of item was removed"
          },
          "replaced_by": {
            "type": ["string", "null"],
            "description": "Replacement ID, or null if demoted to L2 / removed entirely"
          },
          "deprecated_since": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$"
          },
          "note": {
            "type": "string",
            "description": "Migration guidance for downstream consumers"
          }
        }
      }
    },
    "axioms": {
      "type": "array",
      "description": "Formal semantic axioms (OWL 2-compatible) constraining classes and relations",
      "items": {
        "type": "object",
        "required": ["id", "type", "definition"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^ax_[a-z][a-z0-9_]*$",
            "description": "Axiom identifier with ax_ prefix, snake_case"
          },
          "type": {
            "type": "string",
            "enum": [
              "disjoint",
              "cardinality_min",
              "cardinality_max",
              "cardinality_exact",
              "existential",
              "universal",
              "functional",
              "inverse_functional",
              "symmetric",
              "asymmetric",
              "transitive",
              "subproperty"
            ],
            "description": "Axiom type, maps to OWL 2 construct"
          },
          "label_zh": {
            "type": "string",
            "description": "Chinese label"
          },
          "label_en": {
            "type": "string",
            "description": "English label"
          },
          "classes": {
            "type": "array",
            "items": { "type": "string", "pattern": "^[A-Z][a-zA-Z0-9]*$" },
            "minItems": 2,
            "description": "Array of class IDs (used by disjoint axioms)"
          },
          "subject_class": {
            "type": "string",
            "pattern": "^[A-Z][a-zA-Z0-9]*$",
            "description": "The class being constrained (used by cardinality, existential, universal axioms)"
          },
          "relation": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "Relation ID being constrained or characterized"
          },
          "object_class": {
            "type": "string",
            "pattern": "^[A-Z][a-zA-Z0-9]*$",
            "description": "The filler/target class (used by existential, universal axioms)"
          },
          "value": {
            "type": "integer",
            "minimum": 0,
            "description": "Cardinality value (used by cardinality_min, cardinality_max, cardinality_exact)"
          },
          "parent_relation": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "Parent relation ID (used by subproperty axioms)"
          },
          "status": {
            "type": "string",
            "enum": ["stable", "experimental", "deprecated"],
            "default": "stable"
          },
          "since": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$"
          },
          "definition": {
            "type": "string",
            "minLength": 1,
            "description": "Chinese definition"
          },
          "definition_en": {
            "type": "string",
            "description": "English definition"
          }
        }
      }
    },
    "sample_instances": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "type", "label"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "snake_case instance identifier"
          },
          "type": {
            "type": "string",
            "description": "Class ID this instance belongs to — must not reference abstract classes"
          },
          "label": {
            "type": "string",
            "description": "English label"
          },
          "label_zh": {
            "type": "string",
            "description": "Chinese label"
          },
          "status": {
            "type": "string",
            "enum": ["stable", "experimental", "deprecated"],
            "default": "stable",
            "description": "Lifecycle status"
          },
          "since": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$",
            "description": "Version when this instance was first introduced"
          }
        }
      }
    }
  }
}
